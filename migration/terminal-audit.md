# Native terminal admission

The native terminal binds Serial I/O and publishes text input, extended input,
and text output on a terminal child.  Its checked codec handles UTF-8 and escape
input, while lifecycle fixtures cover event, protocol, parent, and child rollback.

The exact retained file at `0x2e3380` keeps GUID
`9e863906-a40f-4875-977f-5b93ff237fc6`, size `0xb046`, PE32/UI/version sections,
and no DEPEX.
